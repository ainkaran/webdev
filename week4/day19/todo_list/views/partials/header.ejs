<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>TODO List</title>
    <!-- when loading css or an image, make sure that value
    for href (or src) refers to the url (even if relative) of
    the file and not the file path -->
    <link rel="stylesheet" href="/css/index.css" />
  </head>
  <body>
    <nav>
      <a href="/posts/new">New TODO List</a> |
      <a href="/posts">TODO List</a> |
      <% if (username) { %>
        <span>Hello, <%= username %>!</span>
      <% } else { %>
        Who are you?
        <form method="POST" action="/username">
          <input type="text" name="username" />
          <input type="submit" value="Submit" />
        </form>
      <% } %>
    </nav>
